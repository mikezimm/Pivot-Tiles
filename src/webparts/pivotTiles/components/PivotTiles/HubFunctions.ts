

import { sp } from "@pnp/sp";
import "@pnp/sp/hubsites";

import { ISite, } from  "@pnp/sp/sites";

import { IHubSite, IHubSiteInfo, IHubSiteWebData } from  "@pnp/sp/hubsites";

export async function getMyHub() {
    // invoke the hub sites object
/*
    const site: ISite = await sp.hubSites.getById("3b4f3aaa-6b8f-4129-849b-86dc7d34464c").getSite();
    const siteData = await site();
    console.log(siteData);
    console.log(siteData.Title);
*/

}